// optimizes ("vecmath.cc:66")
// 2D fill: dst[r, c] = value.

search
    const int r = int(gl_GlobalInvocationID.x);
    if (r >= rllm_bound_x) return;
    const int c = int(gl_GlobalInvocationID.y);
    if (c >= rllm_bound_y) return;
    dst[r, c] = value;

replace
    const int r = int(gl_GlobalInvocationID.x);
    if (r >= rllm_bound_x) return;
    const int c = int(gl_GlobalInvocationID.y);
    if (c >= rllm_bound_y) return;
    dst[r, c] = value;
