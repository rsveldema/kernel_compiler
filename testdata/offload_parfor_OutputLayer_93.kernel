PROGRAM("OutputLayer.cc:93")

OFFLOAD_PARFOR_1D_PARAM(queue, i, limit<2>(), (temp_values))

PARAMETERS
        fixed_size_vector<float, 2>& temp_values

BEGIN
const int i = int(gl_GlobalInvocationID.x);
if (i >= rllm_bound_x) return;
        temp_values[i] = (i == 0) ? -3.402823e38f : 0.0f;

END_PROGRAM